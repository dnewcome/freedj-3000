[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = [
    "aarch64-unknown-linux-gnu",    # Raspberry Pi 5 / Android arm64
    "x86_64-unknown-linux-gnu",     # NUC / dev machine
]
